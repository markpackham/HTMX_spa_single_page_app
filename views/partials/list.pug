//- for dyanmic content we use #{}
//- We push a URL into the browser location history via hx-push-url="true"
each article in articles
  div.article
    h3 #{article.name}
    p #{article.body.slice(0, 100)}...
    a.btn(
      href=`/articles/${article.id}`
    ) View Details